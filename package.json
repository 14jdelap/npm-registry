{
  "name": "hiring-task",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "engines": {
    "node": "^12.0.0"
  },
  "scripts": {
    "start": "nodemon src/main.js",
    "test": "jest --collect-coverage",
    "lint": "eslint --cache '{src,test}/**/*.ts'"
  },
  "dependencies": {
    "express": "^4.17.1",
    "get-port": "^5.1.1",
    "got": "^11.8.1",
    "semver": "^7.3.5"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "nodemon": "^2.0.15"
  }
}
